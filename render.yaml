# Render Blueprint Spec
services:
  - type: web
    name: data-analyst-agent-gem
    env: python
    buildCommand: "pip install -r requirements.txt"
  startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    envVars:
      - key: ENV
        value: production
